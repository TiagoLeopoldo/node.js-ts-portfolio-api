{
  "name": "portfolio-info",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start:dev": "tsx --env-file=.env src/local-server.ts",
    "start:watch": "tsx watch --env-file=.env src/local-server.ts",
    "dist": "rm -rf dist && tsup src && cp src/repositories/categories-profile.json dist/repositories/categories-profile.json",
    "start:dist": "npm run dist && node dist/local-server.js",
    "vercel": "tsx src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.10",
    "dotenv-cli": "^11.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
